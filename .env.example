# Cal.com — server-side API key for availability endpoint and booking creation
# cal.com/settings/developer/api-keys
CAL_API_KEY=cal_live_...

# Site URL (used for OG images, sitemap, structured data)
NEXT_PUBLIC_SITE_URL=https://okamilabs.com

# Stripe — required for /book checkout flow
# Test keys from dashboard.stripe.com/test/apikeys, live keys from dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Beehiiv newsletter — "The Silent Brief" (optional — falls back to /tmp file storage if unset)
# Get these from Beehiiv dashboard → Settings → Integrations
BEEHIIV_API_KEY=
BEEHIIV_PUBLICATION_ID=

# Supabase — partial booking tracking (optional — gracefully disabled if unset)
# Auto-provisioned via Vercel Marketplace, or set manually from supabase.com/dashboard
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# --- Widget (Neon) — Phase 0/1 ---
# Pooler URL for runtime (has "-pooler" in hostname)
DATABASE_URL=
# Direct URL for migrations (no "-pooler")
DATABASE_URL_UNPOOLED=

# --- Widget origin allowlist (Phase 2, production only) ---
ALLOWED_ORIGINS=https://okamilabs.com,https://www.okamilabs.com

# --- Admin basic auth (Phase 3) ---
ADMIN_USER=
ADMIN_PASSWORD_HASH=   # hex(HMAC-SHA256(ADMIN_AUTH_PEPPER, password))
ADMIN_AUTH_PEPPER=     # openssl rand -base64 48

# --- Widget feature gates ---
CHATBOT_ENABLED=0
WIDGET_DISABLED=0

# --- Cookie signing (Phase 7) ---
COOKIE_SECRET=   # openssl rand -base64 48 — REQUIRED in prod (min 32 chars)

# --- Spend cap (Phase 8) ---
DAILY_TOKEN_BUDGET=100000

# --- Sentry (Phase 9, wizard-populated) ---
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=

# --- Phase II (stubbed in Phase I) ---
ANTHROPIC_API_KEY=
AI_MODEL=claude-haiku-4-5
SLACK_WEBHOOK_URL=   # optional lead-capture notifications
